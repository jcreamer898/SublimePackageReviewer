define([
	"backbone", 
	"underscore",
	"views/packages"
], function(Backbone, _, PackgesView) {
	var AppView = Backbone.View.extend({
		el: "body",
		initialize: function() {
			
		}
	});

	return AppView;
});